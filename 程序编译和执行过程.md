# 程序编译和执行过程

###  window中GCC设置

- 找到QT安装目录 ：c:\Qt\Qt5.4.1\5.4\mingw491_32\bin c:\Qt\Qt5.4.1\Tools\mingw491_32\bin 在我的电脑右击选择属性 在高级系统设置选择环境变量 在系统PATH 下将目录添加

### gcc编译程序

* gcc -o hello.exe hello.c



###  分步编译

* 预处理 ： gcc -E hello.c -o hello.i

* 编    译 ： gcc -S hello.i  -o hello.s

* 汇    编 ： gcc -C hello.o -o hello.o
* 链    接 ： gcc     hello.o -o hello



###  //宏定义常量

#include <stdio.h>

#define MAX 100

int msin(void)

{

​	//这是一个行注释

​		/*

​			这也是一个注释

​				*/

​		printf("hello world\n");

​	printf("%d",MAX);

return 0;

}

###  *一步编译*

gcc -o hello.exe hello.c hello2.c





| hello.c     | hello.i      | hello.s               | hello.o              |                                 |
| ----------- | ------------ | --------------------- | -------------------- | ------------------------------- |
| c语言源代码 | 预处理       | 汇编                  | 汇编                 | 链接                            |
|             | 1.宏定义展开 | 1.检查语法            | 1将汇编语言转为c语言 | 将c语言库文件链接到可执行程序中 |
|             | 2.头文件展开 | 2.将c语言转为汇编语言 |                      |                                 |
|             | 3.条件编译   |                       |                      |                                 |
|             | 4.删除注释   |                       |                      |                                 |



常见代码问题问题；

* 编辑时异常
* 编译时异常
* 运行时异常



###  vs中c语言嵌套汇编代码

#include <stdio>

int main()

{

//定义变量a，b，c

int a；

int b；

int c；

__asm

{

mov a,3	//3的值放在a对应内存的位置

mov b,4	//4的值放在b对应内存的位置

moveax,a	//把a内存的值放在eax寄存器

add eax,b	//eax和b相加，结果放在eax

mov e,cax	//eax的值放在c中

}

printf("%d\n",c);	//把

return 0；

} 

##  汇编语言

mov 移动

add 添加

push 压栈

call 调用



eax 32位寄存器

简单简单加法计算：

//__asm

//mov a, 10 

//mov b, 20

//mov eax, b

//

断点和单步调试

断点：在选中行单击或者F9

调试程序： 在“调试”中选择“开始调试”或者“F5”

逐语句执行；F11

逐过程执行；F10

跳出过程：shift+F11

停止调试：shift+F5



##  数据类型

常量与变量

c的关键字























​	